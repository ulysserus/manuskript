В апстриме основной питон-модуль находится в пакете /manuskript, а папки с данными лежат рядом с ней.
Из-за этого установить пакет стандартными макросами rpm-build-python3 вместе с данными через setup.py нельзя.

Поэтому установка ручная:
Скопировать папки с исходниками и данными в %buildroot%python3_sitelibdir/%name (стандартное место для установки пакетов)
Исполняемый питон-файл взять из /package/create_deb апстрима, туда подставить правильный путь до исходников.
